Installation:

    git clone git://github.com/memleaks/vimrc.git ~/.myvim

Switch to the '~/.myvim' directory, and fetch submodules:

    cd ~/.myvim
    git submodule init
    git submodule update

Execute setup script:

    sh setup.sh

Done
